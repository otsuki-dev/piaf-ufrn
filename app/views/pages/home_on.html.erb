<div class="page">
  <div class="content">
    <div class="container-xl">
      <div class="row">
        <div class="col-12">
          <div class="card">
            <div class="card-header">
              <h3 class="card-title">Bem-vindo, <%= current_user.username %>!</h3>
            </div>
            <div class="card-body">
              <p>Você está logado na aplicação.</p>
              <p>Aqui você pode adicionar mais conteúdo, como gráficos, tabelas, etc.</p>
            </div>
          </div>
        </div>
      </div>
    </div>
    <%= render "shared/main" %>
  </div>
</div>

<h1>Nome Aleatório</h1>

<p>O nome escolhido aleatoriamente é: <strong><%= @random_name %></strong></p>

<%= form_with url: names_path, method: :get do |form| %>
  <%= form.label :new_name, "Escolha um novo nome:" %>
  <%= form.select :new_name, @names, include_blank: true %>
  <%= form.submit "Atualizar Nome" %>
<% end %>