<style>
  .btn-floating {
  position: fixed;
  bottom: 20px;
  right: 20px;
  width: 64px;
  height: 64px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.2);
  z-index: 1000;
  padding: 0;
}

  .btn-floating svg {
  width: 36px;
  height: 36px;
  stroke-width: 2.8;
}
</style>
<%= render "shared/main" %>
<%= link_to new_course_path, class: 'btn btn-primary btn-floating' do %>
  <svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
    <path d="M12 5v14"/>
    <path d="M5 12h14"/>
  </svg>
<% end %>


